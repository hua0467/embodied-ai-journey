# 🤖 AI 工具协同作战：企业级项目从零落地的完整方法论

> *你不是在用一个 AI，你是在管理一个 AI 虚拟团队。你的角色从「执行者」变成了「技术经理」。*

---

## 目录

1. [第一性原理](#一第一性原理)
2. [AI 工具矩阵](#二ai-工具矩阵)
3. [六阶段管线法](#三六阶段管线法)
4. [质量控制体系](#四质量控制体系)
5. [实战案例](#五实战案例)
6. [黄金法则](#六黄金法则)
7. [常见陷阱](#七常见陷阱)

---

## 一、第一性原理

### 1.1 为什么单靠一个 AI 做不好企业级项目？

三个硬约束：

| 约束 | 具体表现 | 后果 |
|------|---------|------|
| **上下文窗口瓶颈** | 一个复杂项目涉及几十个文件、上千行业务逻辑，单次对话窗口装不下 | AI 丢失关键上下文，产出前后矛盾 |
| **单模型能力边界** | 没有哪个模型同时擅长代码生成、架构设计、UI 设计、测试编写、文档撰写 | 用一个模型做所有事 = 每件事都做到 60 分 |
| **缺乏质量制衡** | 同一模型生成 + 同一模型审查 = 盲区叠加 | Bug 率大幅上升，边界情况遗漏 |

### 1.2 正确的模型

```
传统方式：你 ──提问──→ [单个 AI] ──回答──→ 你（祈祷它是对的）

正确方式：
                    ┌──→ [AI 研究员] ──→ 调研结论 ──┐
                    │                                  │
你（架构师/PM） ──→ [AI 架构师] ──→ 技术方案 ──→ 你审核 ──→ [AI 前端]
                    │                                  │         ├──→ [AI 后端]
                    │                                  │         ├──→ [AI 测试]
                    └──→ [AI 设计师] ──→ 视觉稿 ──→ 你审核    └──→ [AI 文档]
```

**核心认知**：你的角色从「执行者」升级为「技术经理」。你不再亲自写每一行代码，而是负责需求判断、架构决策、上下文传递、质量把关。

### 1.3 这和你学过的 Agent 知识有什么关系？

这就是 Multi-Agent 范式的人工版：

| Multi-Agent 概念 | 对应本项目中的操作 |
|------------------|-------------------|
| Agent 角色分工 | AI 研究员 / AI 前端 / AI 后端 / AI 测试 |
| Agent 间通信 | 你手动把上一个 AI 的输出复制给下一个 AI |
| 任务编排（Orchestration） | 六阶段管线法 |
| 反馈循环（Feedback Loop） | 对抗式审查（AI-A 生成 → AI-B 审查 → AI-A 修正） |
| 上下文管理 | PRD + 技术方案文档 = 所有 AI 的共享记忆 |

---

## 二、AI 工具矩阵

### 2.1 团队分工表

```
┌─────────────────┬──────────────────────────────┬──────────────────┐
│ 角色             │ 推荐工具                     │ 在项目中的职责    │
├─────────────────┼──────────────────────────────┼──────────────────┤
│ 🧠 架构师/PM     │ Claude (Sonnet/Opus)         │ 需求拆解、技术   │
│                  │ ChatGPT (GPT-4o/o1/o3)       │ 选型、系统设计   │
├─────────────────┼──────────────────────────────┼──────────────────┤
│ 🔍 研究员        │ Perplexity Pro               │ 竞品调研、技术   │
│                  │ Gemini Deep Research         │ 验证、最新文档   │
├─────────────────┼──────────────────────────────┼──────────────────┤
│ 💻 后端工程师    │ Claude Code / Cursor         │ API 开发、数据库 │
│                  │ GitHub Copilot               │ 设计、业务逻辑   │
├─────────────────┼──────────────────────────────┼──────────────────┤
│ 🎨 前端工程师    │ v0 (Vercel) / Bolt / Lovable │ 页面生成、组件   │
│                  │ Cursor + Claude              │ 开发、交互实现   │
├─────────────────┼──────────────────────────────┼──────────────────┤
│ 🖼️ 设计师        │ Midjourney / DALL-E          │ Logo、Banner、   │
│                  │ Figma AI                     │ 插图等视觉素材   │
├─────────────────┼──────────────────────────────┼──────────────────┤
│ 🧪 测试工程师    │ Claude / ChatGPT             │ 测试用例生成、   │
│                  │ Playwright + AI              │ 自动化测试       │
├─────────────────┼──────────────────────────────┼──────────────────┤
│ 📝 文档工程师    │ Claude / Notion AI           │ API 文档、用户   │
│                  │                              │ 手册、README     │
├─────────────────┼──────────────────────────────┼──────────────────┤
│ 🔗 集成/部署     │ Claude Code / Bolt           │ CI/CD 脚本、     │
│                  │ Vercel / Railway / Docker    │ 环境配置、部署    │
└─────────────────┴──────────────────────────────┴──────────────────┘
```

### 2.2 工具选择决策树

```
项目需要什么？ 
  ├── 需求还不清晰？ 
  │   └── 先用 Claude/ChatGPT 做需求访谈（阶段 1）
  ├── 需要调研竞品/查最新技术？ 
  │   └── Perplexity Pro / Gemini Deep Research
  ├── 需要生成 UI 页面？ 
  │   └── v0（偏开发者）/ Bolt（偏全栈快速原型）
  ├── 需要写后端代码？ 
  │   └── Claude Code（命令行）/ Cursor（IDE 内）
  ├── 需要设计素材？ 
  │   └── Midjourney（高质量图片）/ DALL-E（快速生成）
  ├── 需要写测试？ 
  │   └── Claude（粘贴代码 + "为以下代码生成测试"）
  └── 需要部署？ 
      └── Railway（全栈简单部署）/ Vercel（前端优先）
```

---

## 三、六阶段管线法

> **这是本文的核心。六个阶段，每个阶段有明确的输入、AI 工具、输出和质量门。**

### 阶段 1：需求澄清与架构设计

**目标**：从一句模糊需求 → 一份可执行的 PRD + 技术方案

**负责 AI**：Claude（架构师角色）+ Perplexity（研究员角色）

```
步骤 1.1 — 需求访谈
─────────────────────
[工具] Claude
[操作] 新开一个对话，发送：

"你现在是一个有 10 年经验的 B 端产品经理。我的业务场景是
 [描述你的场景，如：工业巡检机器狗的数据管理平台]。
 请像访谈客户一样，分 5 轮问我问题，每轮 3-4 个问题，
 帮我澄清需求边界、用户画像、核心功能、非功能需求。
 每轮等我回答后再继续下一轮。"

步骤 1.2 — 竞品调研
─────────────────────
[工具] Perplexity Pro
[操作] 搜索：

"搜索 2024-2025 年 [你的领域] 的竞品产品或平台，
 列出 Top 5，分析：核心功能、技术栈、定价模式、优缺点。
 以表格形式输出。"

步骤 1.3 — 技术方案输出
─────────────────────
[工具] Claude
[操作] 将步骤 1.1 和 1.2 的结论粘贴进去，发送：

"基于以下需求和竞品分析 [粘贴内容]，请输出：
 1. 推荐技术栈及理由（前端/后端/数据库/部署）
 2. 系统架构图（Mermaid 语法）
 3. 核心数据模型 ER 图
 4. API 路由设计（RESTful 或 GraphQL）
 5. 三阶段开发路线图（MVP / V1.0 / V2.0）"
```

**产出物**：

- `PRD.md`（产品需求文档）
- `TD.md`（技术设计文档）

**⚠️ 质量门**：你亲自通读两份文档，确认逻辑自洽。这个阶段**绝对不能跳过**——后续所有 AI 对话都依赖这份上下文。

---

### 阶段 2：UI/UX 设计

**目标**：从技术文档 → 可交互的前端原型

**负责 AI**：v0 / Bolt + Midjourney + Claude

```
步骤 2.1 — 页面生成
─────────────────────
[工具] v0 (v0.dev) 或 Bolt (bolt.new)
[操作] 输入 Prompt：

"基于以下 PRD [粘贴步骤 1 中的页面功能列表]，生成完整的前端页面。
 包含：登录页、仪表盘、[核心页面1]、[核心页面2]...
 技术栈：React + Tailwind CSS + shadcn/ui
 每个页面包含 loading、empty、error 三种状态。"

步骤 2.2 — 视觉素材
─────────────────────
[工具] Midjourney 或 DALL-E
[操作] 生成 Logo、Banner 图、空状态插图等

步骤 2.3 — 交互审查
─────────────────────
[工具] Claude
[操作] 把 v0 生成的页面截图（或链接）的描述发给 Claude：

"审查这个 [页面名] 的交互设计，从用户体验角度指出
 3-5 个最大的问题和改进建议。按严重程度排序。"
 
 然后用建议回 v0 迭代。
```

**产出物**：可交互的前端原型链接

**⚠️ 质量门**：找一个同事/朋友试用原型，收集真实反馈，迭代一轮。

---

### 阶段 3：后端开发

**目标**：从前端原型 → 能跑的 API 服务

**负责 AI**：Claude Code / Cursor

> **关键技巧：不要让 AI 一次性生成所有代码。按模块逐个来，每个模块验证通过后再进入下一个。**

```
步骤 3.1 — 数据库 Schema
─────────────────────
[工具] Claude Code
[操作] Prompt：

"根据以下数据模型 [粘贴阶段 1 的 ER 图]，生成 PostgreSQL 建表语句。
 包含所有索引、外键约束、时间戳触发器。
 同时生成 Prisma / SQLAlchemy 的 ORM Schema。"

步骤 3.2 — 逐模块开发 API
─────────────────────
[工具] Claude Code / Cursor
[操作] 严格按以下顺序，一个模块完成后测试通过再继续：

模块 1："实现用户认证：注册、登录、JWT Token 签发与验证中间件"
模块 2："实现 [核心实体] CRUD 接口，含分页、排序、筛选"
模块 3："实现 [关联功能] 接口，处理外键关联和数据级联"
模块 N："实现 [下一个功能]..."

步骤 3.3 — 测试生成
─────────────────────
[工具] Claude
[操作] 每完成一个模块：

"为上述 [模块名] 代码生成 pytest/jest 测试用例，覆盖：
 1. 正常路径
 2. 参数边界值
 3. 权限异常
 4. 数据库异常
 输出可直接运行的测试代码。"
```

**产出物**：能跑的后端 API + 测试套件

**⚠️ 质量门**：`npm test` 或 `pytest` 全部通过。用 Postman/Bruno 手动调通 5 个核心接口。

---

### 阶段 4：前后端联调

**目标**：前端 + 后端 = 一个完整的产品

**负责 AI**：v0（前端调整）+ Claude（Bug 排查）

```
步骤 4.1 — 导出 API 文档
─────────────────────
[工具] 你的后端框架自带的 Swagger/OpenAPI 导出
[操作] 访问 http://localhost:8000/docs 或 /openapi.json，
        导出为 JSON 文件。

步骤 4.2 — 前端对接
─────────────────────
[工具] v0 或 Claude
[操作] 把导出的 API 文档粘贴进去：

"这是后端的完整 API 文档。请把所有页面中的 mock 数据替换为
 真实 API 调用。每个接口需要处理 loading、empty、error 三种状态。
 错误时显示用户友好的提示信息。"

步骤 4.3 — Bug 排查
─────────────────────
[工具] Claude
[操作] 遇到 Bug 时，用标准化格式提问：

"前端调用 [POST /api/xxx] 时，请求体是 [{...}]，
 后端返回 [500 Internal Server Error]。
 相关后端代码如下：
 [粘贴代码]
 请分析可能原因，给出排查步骤和修复方案。"

修复 → 重新测试 → 通过 → 下一个 Bug
```

**⚠️ 质量门**：核心业务流程端到端走通。至少覆盖：登录 → 列表页 → 详情页 → 创建/编辑 → 返回。

---

### 阶段 5：部署上线

**目标**：localhost → 生产 URL

**负责 AI**：Claude Code + 部署平台（Railway/Vercel）

```
步骤 5.1 — 生成部署配置
─────────────────────
[工具] Claude Code/ChatGPT

"我的技术栈：[前端框架] + [后端框架] + [数据库]。
 请生成部署到 [Railway / Vercel / AWS] 的完整配置：
 1. 环境变量清单（每个变量的用途说明）
 2. 部署配置文件
 3. CI/CD 脚本（Git push → 自动部署）
 4. 数据库迁移脚本"

步骤 5.2 — Docker 化（推荐）
─────────────────────

"为这个项目生成 Dockerfile 和 docker-compose.yml：
 - 前端一个容器（Node.js + Nginx）
 - 后端一个容器（Python/Node.js）
 - 数据库一个容器（PostgreSQL）
 - 容器间网络配置"

步骤 5.3 — 执行部署
─────────────────────
按照生成的配置，在 Railway/Vercel 上点击 Deploy。
部署成功后，用生产环境 URL 完整走一遍核心流程。
```

**⚠️ 质量门**：你可以在手机上用 4G 网络访问生产环境，完成核心操作。

---

### 阶段 6：文档与交付

**目标**：项目 → 可交付物

**负责 AI**：Claude

```
步骤 6.1 — API 文档
─────────────────────
"基于项目代码，生成完整的 API 文档（Markdown）：
 每个接口：路径、方法、请求/响应示例、错误码表"

步骤 6.2 — 用户手册
─────────────────────
"基于项目功能，写一份非技术人员能看懂的操作手册。
 每个核心操作配步骤说明（1-2-3 步格式）。
 用截图占位符标注哪里需要配图。"

步骤 6.3 — 技术复盘
─────────────────────
"回顾整个项目，输出：
 1. 技术债务清单（需重构的部分）
 2. 安全审查要点
 3. 性能优化建议（含预计收益）
 4. 遗留问题与建议解决方案"
```

---

## 四、质量控制体系

### 4.1 对抗式审查（最重要的一条）

> **永远不要让同一个 AI 审查自己生成的内容。**

```
操作流程：
1. [Claude 对话 A] 生成代码/设计/文档
2. [Claude 对话 B，或换用 ChatGPT] 
   Prompt："请以资深 [后端/前端/安全] 工程师身份，严格审查以下内容。
           找出所有问题，对每个问题标注严重程度（致命/高/中/低）
           并给出具体修复建议。"
3. [回到 Claude 对话 A] 贴上审查结果，让它逐个修复
4. 你确认修复结果
```

**为什么有效**：代码 review 的原理——两个人的盲区不重叠。同样的原理适用于 AI。

### 4.2 边界遍历测试

```
Prompt：
"为 [功能模块] 设计全面的测试场景，覆盖：
 1. 正常路径（Happy path）
 2. 边界值（空、超长、0、负数、特殊字符、Unicode）
 3. 异常情况（网络超时、数据库断开、并发写入冲突）
 4. 权限矩阵（未登录/已登录无权限/跨租户访问）
 5. 状态机转换（每种状态 → 每种状态是否合法）
 
 输出为可直接运行的测试代码文件。"
```

### 4.3 甲方视角验收

```
Prompt：
"假设你是这个项目的甲方，明天验收付款。请站在一个挑剔的客户角度：
 1. 列出你会检查的 20 个验收点
 2. 逐项自测这个项目，给出：✅ 通过 / ❌ 不通过 / ⚠️ 有条件通过
 3. 对于不通过和有条件通过的项，给出修改建议"
```

---

## 五、实战案例

### 案例：机器狗巡检数据看板

**背景**：公司需要做一个 Web 端管理后台，让客户能查看机器狗的巡检数据、设备状态、告警记录。

**执行过程**：

```
第 1 天（~2h）
├─ [Claude] 需求访谈 → 输出功能清单 + 用户画像
├─ [Perplexity] 调研宇树/云深处/Boston Dynamics 的数据平台
└─ [Claude] 输出 PRD + 技术方案
          技术选型：Next.js + FastAPI + PostgreSQL + Railway

第 2 天（~3h）
├─ [v0] 生成 4 个核心页面（仪表盘/设备列表/巡检地图/告警中心）
├─ [Midjourney] 生成 Logo + 空状态图
└─ [Claude] 审查交互逻辑 → 12 条改进建议 → v0 迭代 2 轮

第 3 天（~4h）
├─ [Claude Code] 创建数据库 Schema + Prisma ORM
├─ [Claude Code] 逐模块开发 API
│   ├── 用户认证（JWT）
│   ├── 设备管理 CRUD
│   ├── 巡检记录上传 + 查询（分页/筛选）
│   └── 告警规则 + 通知
└─ [Claude] 为每个模块生成 pytest 测试

第 4 天（~3h）
├─ 导出 Swagger JSON → [v0] 替换 mock 为真实 API
├─ [Claude] 联调中排查修复 5 个 Bug
└─ [你] 手动走通全流程

第 5 天（~2h）
├─ [Claude Code] 生成 Docker Compose + Railway 配置
├─ [Railway] 一键部署
├─ [Claude 对话 B] 对抗式代码审查 → 修复 3 个安全问题
└─ [Claude] 生成 API 文档 + 用户手册
```

**总投入**：约 14 小时（MVP 级别）
**产出**：可访问的生产环境 URL + 完整文档 + 测试覆盖

---

## 六、黄金法则

> **法则 1：AI 做 80%，你做 20%——但这 20% 是决定性的。**
>
> AI 负责：代码、设计、文档、测试。
> 你负责：需求判断、架构决策、质量把关、跨 AI 上下文传递。
> 这 20% 决定了项目是「能用」还是「优秀」。

> **法则 2：一个 AI 生成，另一个 AI 审查。**
>
> 成本最低、收益最高的质量提升手段。换模型或新开会话，
> 把产出交给另一个 AI 审查。盲区不重叠，Bug 率大幅下降。

> **法则 3：PRD + 技术方案是所有 AI 的共享记忆。**
>
> 阶段 1 输出的文档，在阶段 2-6 的每一次 AI 对话中都要贴进去。
> 不要偷懒。AI 没有跨对话记忆，你作为上下文管理器，
> 每次都要重新建立它的认知。

> **法则 4：逐模块推进，不要一口吃成胖子。**
>
> 「帮我做一个完整的后台系统」→ 产出是屎山。
> 「先做用户认证模块」→ 验证通过 →「再做设备管理」→ ...
> 这是软件工程的模块化原则，对 AI 同样适用。

> **法则 5：先跑通，再优化。先 MVP，再迭代。**
>
> 不要试图在第一轮就让 AI 生成完美代码。
> 让它先生成能跑的版本，你跑通核心流程，再一轮一轮优化。
> 完美的代码不存在，能用的代码可以变得更好。

---

## 七、常见陷阱

| 陷阱 | 表现 | 解决方案 |
|------|------|---------|
| **上下文溢出** | AI 生成到一半开始胡说八道 | 拆分对话，一个模块一个对话；关键上下文每次都重新贴 |
| **过度信任** | 直接复制 AI 代码上线，没测试 | 永远假设 AI 的代码有 Bug。先本地跑通再部署 |
| **一次性需求** | 「帮我做一整个后台」 | 拆成独立模块，逐模块提问 |
| **不贴上下文** | 每次问 AI 都不给背景信息 | 把 PRD 和技术方案的摘要复制进每一次对话 |
| **只用一种工具** | 所有问题都问同一个 AI | 不同任务用擅长的工具：设计用 v0，调研用 Perplexity，代码用 Claude Code |
| **不迭代** | AI 生成第一版直接交付 | 第一版是毛坯房，至少迭代 2-3 轮才能入住 |
| **忘记安全审查** | 代码能跑就上线 | 上线前专门开一个对话做安全审查（SQL 注入、XSS、权限绕过） |

---

> **最后一句**：这套方法论的核心不是工具列表，是一种思维切换——**从「我能不能做」变成「我怎么安排 AI 团队做」**。前者让你成为一个人的瓶颈，后者让你拥有一个团队的能力。
>
> 你在具身智能实习中学到的 Agent 范式（任务拆解、工具调用、反馈循环、Multi-Agent 协作），会反过来帮你更好地驾驭这套 AI 工具链。这是一个奇妙的双向增强。

---

*2026.08 | 启航机器人实习工作笔记 | AI 工具使用专题*
