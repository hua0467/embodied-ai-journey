# 从手到数据：VR 遥操管线到底在跑什么

> 上一篇写了 π₀.₅ 要什么格式的数据。这篇往前退一步——数据是怎么从你的手变成训练集里那一行行数字的。
> 导师让我别着急看代码，先把整条链路吃透。以下就是吃透的结果。

---

## 你在干什么

你要让 F1 机械臂学会「拿方块」。但不写代码控制它——你用手演示给它看，它看多了就会了。

这事的学名叫模仿学习（Imitation Learning）。核心逻辑就一句话：**人做一遍 → 录下来 → 模型学 → 模型复现。**

听起来简单，但从「你的手在空气中动」到「模型吐出一行 Parquet」中间过了四层，每层都有一堆你以为不重要的细节。

---

## 第一层：Quest 3 怎么看你的手

Quest 3 头显上有 4 颗黑白摄像头。它们不拍照——它们实时算你的手在空间里的精确位置。

手上识别出 21 个关键点（每根手指的三个关节 + 指尖 + 手腕），每个点一个 (x, y, z) 三维坐标。所以一只手一帧是 21×3 = 63 个浮点数，再加上手腕的 7 个数（位置 xyz + 旋转四元数 xyzw）。

```
拇指尖: (0.12, 0.35, -0.05)
食指尖: (0.08, 0.30, -0.02)
  ...
手腕:   (0.00, 0.20, 0.10, 0, 0, 0, 1)   ← 位置 + 旋转
```

一帧 70 个数，两只手 140 个。30 帧每秒，光是手部数据就是每秒 4200 个浮点数。

**这层不需要你管。** Quest 3 系统自带的，Meta 调好了。你知道它输出什么就行。

---

## 第二层：数据怎么从 Quest 3 飞到你电脑

我们装的那个 Hand Tracking Streamer 应用，做的事就是当搬运工：

1. 从 Quest 3 系统拿手部追踪数据
2. 打包成一串逗号分隔的数字（CSV 格式的 UDP 包）
3. 通过 WiFi 广播到你电脑的 9000 端口

### 为什么非要用 WiFi，USB 线不行吗

因为 Hand Tracking Streamer 把自己定位成一个「无线传感器」。它走 UDP 网络协议广播数据——同一局域网里任何设备都能收。你的电脑收、仿真同学的电脑也能同时收，互不影响。

USB 线在这个架构里只负责装软件和调试。装完了，真正的数据传输就切到 WiFi 了。你可以把它理解成：**装完打印机驱动之后，你把 U 盘拔了，打印机走 WiFi 连。** Quest 3 也一样——APK 装完，线就可以拔了，后面全走无线。

### UDP 是什么

一种「我发我的，你收到没有我不管」的通信方式。像广播电台——电台只管播，你听不听、听到多少，它不在乎。好处是极快，延迟十几毫秒——你手动，Quest 3 马上算，数据瞬间到电脑。坏处是偶尔丢一两个包你都不知道。

对于遥操作来说，这个取舍是对的：我宁愿偶尔丢一帧，也不想要 100ms 的延迟——延迟高了你的手和机器人的动作就脱节了。

**这层你也基本不用管。** 只要 Quest 3 和电脑在同一个 WiFi 下，应用配好 IP 和端口，它就自己跑。

---

## 第三层：电脑收到数据后干了什么

你的 PC 上跑着一个 Python 脚本，它做两件事：

### 第一件事：接收

用 `hand-tracking-sdk` 监听 UDP 9000 端口。SDK 帮你把那一串逗号分隔的数字翻译成结构化的 HandFrame 对象——左手还是右手、手腕位姿、21 个关节坐标，一目了然。

### 第二件事：录制

以固定的频率（比如 30 FPS）把每一帧的数据写入 HDF5 文件。HDF5 就是一种能高效存大规模数组的文件格式，科研和机器人领域用了几十年。比纯文本快，比数据库轻。

每一帧录什么：

```
timestamp:      0.033   ← 从开始到现在的秒数
观测数据:
  左手 21 个关节点坐标  (63 个数)
  左手手腕位姿          (7 个数)
  右手 21 个关节点坐标  (63 个数)
  右手手腕位姿          (7 个数)
  机器人当前关节状态    (6 个数，现在还空着——等接入 F1 后才填)
动作数据:
  机器人目标关节角度    (6 个数，现在还空着——等有了 IK 解算后才填)
元数据:
  这是第几条演示
  是不是最后一帧
  成功还是失败
```

**关键点：** 现在只录了手部数据（observation），机器人的动作数据（action）还是空的。因为中间还缺一步——把手的位姿映射成 F1 机械臂的关节角度。这叫 **IK 解算（逆运动学）**，需要 F1 的 URDF 模型文件。这是下周的事。

但在数据格式的设计上，已经提前留好了 action 的位置——填进去就行，框架不用改。

---

## 第四层：为什么最后要转成 LeRobot 格式

π₀.₅ 不认识 HDF5，它只吃 LeRobot 格式。

你的转格式脚本做的事就是翻译：

```
HDF5（你的草稿本）→ Parquet（交作业的正式答卷）
```

Parquet 是列式存储——同一列的数据挨着存，压缩率高、机器学习训练时读取极快。对比一下：行式存储（比如 CSV）读一行等于所有列都要扫一遍；列式存储只读你训练需要的那几列。一个数据集几十万行，差一个数量级。

除了 Parquet，还得补三份元数据：

- `info.json`：列名、类型、维度，相当于数据集的说明书
- `episodes.jsonl`：每条演示从哪帧开始到哪帧结束
- `stats.json`：归一化统计量，训练时必须

**这一层我们已经完全跑通了。** 模拟数据进去，标准格式出来，验证通过。

---

## 一张图收尾

```
你的手在空中动
    │
    ▼
[Quest 3 摄像头] → 算出 21×3 个 3D 坐标
    │
    ▼
[Hand Tracking Streamer] → UDP 包 → WiFi → 飞到你电脑
    │
    ▼
[PC Python 脚本] → 接收 + 按 30FPS 录制 → HDF5 文件
    │
    ▼
[格式转换] → HDF5 → Parquet + info.json → LeRobot 数据集
    │
    ▼
[归一化统计] → 算 q01/q99 → stats.json
    │
    ▼
[π₀.₅ 后训练] → 模型学会「手这样动 = 机械臂应该这样动」
```

---

## 现在到哪了

| 环节 | 状态 |
|------|------|
| Quest 3 识别你的手 | ✅ 系统原生支持 |
| Hand Tracking Streamer APK | ✅ 已装好 |
| PC 端接收脚本 | ✅ 就绪 |
| UDP 数据流入 | ❌ Quest 3 没 WiFi |
| HDF5 录制 | ✅ 模拟数据通过 |
| LeRobot 转换 | ✅ 格式验证通过 |
| IK 解算（手→机械臂） | ⏳ 等 F1 URDF |
| π₀.₅ 后训练 | ⏳ 等数据就绪 |

真正的阻塞点就一个：WiFi。除此以外整条链路是通的。

---

> 这个系列的下一篇应该是「终于连上了 WiFi，第一帧真实数据是什么样的」或者「拿到 URDF 之后怎么把手部位姿变成机器人关节指令」——取决于哪个先发生。

---

*写于 2026.8.7 | VR 遥操数据管线学习笔记*
