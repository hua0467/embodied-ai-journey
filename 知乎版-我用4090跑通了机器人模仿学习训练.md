# 我用 4090 跑通了机器人模仿学习训练

**副标题：LeRobot ACT 训练链路验证实录**

> 我是一名汽车服务工程专业的实习生，在一家机器人初创公司做具身智能研发助理。公司把一台 48GB 显存的 RTX 4090 主机交到我手上，我把这台机器搭成了自己的 AI 实验平台，花一天时间从零跑通了 LeRobot + ACT 机器人模仿学习训练链路。环境怎么配、坑怎么踩、结果什么样，这篇文章全记录。

## 一、我是谁，为什么做这件事

我是汽车服务工程专业的大四学生，现在在深圳一家机器人初创公司做具身智能研发助理。我负责的是 VR 遥操作数据采集模块：人戴着 VR 设备控制真机机器人，把人的动作录下来，作为训练机器人策略的"教材"。

我所在的链条是：采数据 → 转格式 → 喂模型训练。前两段我很熟，但训练这一段一直是别人在做。有个问题一直悬着：**没亲手跑通过训练，我对"数据该怎么采"的理解就是二手的。**

我手上有一台 48GB 显存的 RTX 4090 主机，公司把它交给我负责。我把这台机器当作自己的 AI 实验平台，给自己定了一个很小的目标：**在这台机器上，独立完成一次完整的机器人策略训练。**

先用官方示例数据跑通链路，下一步换成我自己采的真机数据。

## 二、环境配置：从裸机开始

机器是 Ubuntu 22.04，nvidia-smi 显示显存 49140MiB（约 48GB），CUDA 13.2 驱动。配置流程：

1. 装 Miniforge3，创建 Python 3.10 环境，取名 robot
2. 装 PyTorch 2.6.0（CUDA 12.4 版），验证 `torch.cuda.is_available()` 返回 True
3. 装 LeRobot——HuggingFace 出的机器人学习框架
4. 下载官方示例数据集：50 个 episode、25000 帧、14 维动作、50fps 视频

听起来就四步，实际踩了 8 个坑，占了今天大部分时间。

## 三、8 个坑，没有一个是算法问题

1. **conda 命令找不到**——PATH 没配，`conda init bash` 解决
2. **pip 装的 lerobot 没有训练脚本**——训练脚本只在源码里，改成源码安装
3. **源码主分支要求 Python ≥ 3.12**——`git checkout v0.4.4` 切兼容版本
4. **训练脚本模块名不对**——是 `lerobot.scripts.lerobot_train`，不是 `lerobot.scripts.train`
5. **默认要把模型推到 HuggingFace Hub**——加 `--policy.push_to_hub=false`
6. **缺 gym-aloha 依赖**——`pip install gym-aloha`
7. **ResNet18 预训练权重下载 SSL 失败**——手动下载 45MB 权重放进缓存目录
8. **默认视频解码器报缺 FFmpeg 库**——改用 pyav

有个感受很真实：**这 8 个问题没有一个是算法问题，全是环境、版本、路径、默认参数。** 训练链路能不能跑通，瓶颈从来不在模型，在工程细节。而工程细节，恰好是愿意动手的人才能攒下来的东西。

## 四、训练结果：2000 步，loss 从 7.3 到 1.27

先 10 步验证链路，再 500 步，最后 2000 步。命令长这样（W&B 关掉，视频解码用 pyav）：

```bash
WANDB_MODE=disabled python -m lerobot.scripts.lerobot_train \
  --policy.type=act \
  --env.type=aloha \
  --dataset.repo_id=~/lerobot_data/aloha_sim_insertion_human \
  --policy.device=cuda \
  --policy.push_to_hub=false \
  --dataset.video_backend=pyav \
  --output_dir=~/lerobot_output/act_test_2000 \
  --steps=2000
```

2000 步 80 秒跑完，约 25 step/s。loss 变化：

| step | loss |
|---|---|
| 200 | 7.315 |
| 400 | 3.047 |
| 600 | 2.502 |
| 800 | 2.209 |
| 1000 | 2.000 |
| 1200 | 1.803 |
| 1400 | 1.648 |
| 1600 | 1.492 |
| 1800 | 1.380 |
| 2000 | 1.266 |

loss 单调下降、没有振荡。1.27 离收敛还远，2000 步只是热身——但这不重要。重要的是：**checkpoint 正常落盘，训练、保存的整条机制在我手上是通的。** 模型参数量 5161 万（约 52M），对单机后训练非常友好。

## 五、几个技术细节

- **为什么是 ACT**：Action Chunking Transformer，模仿学习的经典策略，用 Transformer 一次预测一串动作，适合"看视频学动作"的场景
- **为什么源码安装 LeRobot**：pip 成品包只有库文件，训练脚本在源码仓库里；选 v0.4.4 是因为主分支已要求 Python 3.12
- **为什么视频解码用 pyav**：默认的 torchcodec 依赖系统 FFmpeg 共享库，训练机没装；pyav 是纯 Python 轮子，更省心
- **25 step/s 意味着什么**：5 万步约 33 分钟。正式训练以数万步计，这台机器完全扛得住

## 六、对想入行具身智能的同学说几句

1. **学历背景不是障碍，动手才是硬通货。** 团队里不缺算法博士，缺的是愿意把环境配好、把坑一个个踩完的人。
2. **从最小目标开始。** 我今天的全部目标就是"跑通"——2000 步、80 秒。跑通之后再谈规模。
3. **把每个坑记下来。** 8 个问题我都写进了 GitHub：报错原文 + 原因 + 解法。这些记录就是你的作品集。
4. **把交到你手里的资源做到极致。** 一台 4090 到了你手里，跑通它、跑出结果，它就是你的简历。

---

**关于我**：汽车服务工程专业大四学生，具身智能研发助理。目标：去香港读研，方向具身智能与机器人学习。这次训练的完整记录——环境配置、全部命令、8 个坑的详细解法、分日训练日志——都在我的 GitHub 仓库 `LeRobot-ACT训练实录` 目录里：

👉 [github.com/hua0467/embodied-ai-journey](https://github.com/hua0467/embodied-ai-journey)

欢迎交流、指正。
