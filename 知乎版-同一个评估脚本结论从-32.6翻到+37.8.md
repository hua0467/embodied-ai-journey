# 同一个评估脚本，结论从 -32.6% 翻到 +37.8%

**副标题：给机器人 VLA 模型做 LoRA 微调的一天**

> 我是一名汽车服务工程专业的大四学生，在机器人初创公司做具身智能研发助理。公司把一台 48GB 显存的 RTX 4090 交到我手上负责。
>
> 这一天里，我用 LoRA 给机器人视觉-语言-动作模型做了微调：只训练 2.5% 的参数，2000 步 8 分 42 秒。评估时差点把"变差 32.6%"写进报告——最后发现是我把两个坐标系搞混了。修正后，微调让预测动作与真值的距离下降 37.8%。

## 为什么是 LoRA

给机器人选策略，像挑车：ACT 是皮实面包车（5200 万参数，2000 步 1 分 22 秒）；Diffusion Policy 是性能轿跑（2.6 亿参数，拟合精度最高）；SmolVLA 是能听懂人话的智能座舱——它天生吃语言指令，这正是我做 VR 遥操数据采集的方向。

定下 SmolVLA 后，全参数微调一次要动 1 亿参数。LoRA 的思路用修车的话说：**发动机变速箱都不动，只加装一套小零件**。最终可训练参数 1152 万，占 4.6 亿总参数的 2.5%，checkpoint 只有 125MB。

## 第一个坑：训练和"挂载旧零件"是两条路

第一次跑我加了 `--policy.use_peft=true`，看着顺理成章。

报错：`Can't find 'adapter_config.json'`。

排查半天才读懂：这个参数的意思是"加载一个已经训练好的适配器"（推理场景），训练时新建 LoRA 只传下面这些就行：

```bash
--peft.method_type=lora --peft.r=16 --peft.target_modules=all-linear
```

一个是"装新零件"，一个是"检查旧零件"。删掉多余的参数，2 步冒烟通过，再放 2000 步。

## 数据没到位：两个网段之间没有路

傍晚换自己的数据：训练机在办公室 WiFi 网段，机器人自带路由器在另一个网段，两网段无路由互通，现场没网线。

数据过不来。我决定不等——用公开真实遥操数据集 `aloha_static_coffee`（50 集、55000 帧、含语言指令）当替身，1.57GB 下载验证，直接开训。**loss 0.982 → 0.108，单调收敛，没有过拟合。**

## 评估翻盘：扭矩扳手的单位搞错了

我加了一个硬指标：预测动作 vs 真值动作的 L2 距离。距离下降 = 输出更贴近人类演示。

首版结果：**微调后距离上升 32.6%。** 心里一沉，正准备写进报告，顺手多查了一步——模型输出在**归一化空间**，真值在**原始动作空间**。两边根本不在一个坐标系。

就像扭矩扳手显示 N·m，维修手册写 kgf·m。**数字都对，结论全错。**

用数据集的 mean/std 反归一化后重跑：**距离下降 37.8%**。同一个脚本，同一个数据，结论翻盘。

这是我今天最大的一课：评估方法学比模型本身更容易出错。以后任何"预测 vs 真值"的对比，先问一句——两边在不在同一个坐标系。

## 一个诚实的注脚

替身数据很可能就在 SmolVLA 的预训练语料里——37.8% 是在"已知任务"上榨出来的。驾校考试车练熟了，不代表开自己的车就顺。等自采数据到位重跑，才能看到真正的微调收益。

但今天的价值不在数字：**微调管线（数据验证 → LoRA 训练 → 双指标评估）完整跑通了**，数据一到，换一个参数就能复用。

---

**关于我**：汽车服务工程专业大四学生，具身智能研发助理，负责 VR 遥操数据采集与训练链路。正在把踩过的坑和跑通的链路写成公开笔记：

GitHub：[github.com/hua0467/embodied-ai-journey](https://github.com/hua0467/embodied-ai-journey)（本篇完整版 + LoRA 技术笔记）
